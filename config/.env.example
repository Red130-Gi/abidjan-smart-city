# Configuration for Abidjan Smart City Platform
# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC_TRAFFIC=traffic_data
KAFKA_TOPIC_WEATHER=weather_data
KAFKA_TOPIC_INCIDENTS=incident_alerts
KAFKA_TOPIC_PREDICTIONS=traffic_predictions

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=smart_city

# MongoDB Configuration
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USER=admin
MONGO_PASSWORD=password
MONGO_DB=smart_city_logs

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
JWT_SECRET_KEY=supersecretkey_change_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Spark Configuration
SPARK_MASTER=spark://localhost:7077

# Grafana Configuration
GRAFANA_PORT=3000
